requests>=2.31
Pillow>=10.0
numpy>=1.26

# The wall's ears: Shazam signatures, built in Rust. audioop-lts is the
# module pydub (inside shazamio) still imports and Python 3.13 removed.
shazamio
audioop-lts; python_version >= "3.13"

# HomeKit: the wall in the Home app (brain/homekit.py). The brain runs
# without it and says so when [homekit] is enabled; the QRCode extra draws
# the pairing code on the panel.
HAP-python[QRCode]

# The fallback for videos YouTube refuses the wall's own resolver (VIDEO.md).
# Optional: the wall runs without it, and says so when a video needs it.
# Needs updating often; pi/install-ytdlp.sh is the one-liner for that.
yt-dlp

# Ask the wall: official Claude SDK, adaptive thinking and wall tools.
anthropic>=0.70

# Local wake inference uses ONNX only. pi/install-voice.sh installs these
# plus the pinned openWakeWord source with --no-deps, avoiding unused TFLite.
onnxruntime>=1.20,<2; python_version < "3.14"
scipy>=1.14,<2
scikit-learn>=1.5,<2
tqdm>=4,<5
# Native whisper.cpp and tiny/base model files: bash pi/install-voice.sh.
